[Unit]
Description=ROS2 device driver container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker run --network host --privileged openvins:side_right
ExecStop=/usr/bin/docker stop -t 2 openvins:side_right

[Install]
WantedBy=default.target
